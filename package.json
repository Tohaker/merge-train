{
  "name": "merge-train",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebuild:zip": "mkdirp --mode=0700 build",
    "build:zip": "jszip-cli -c functionzip.json",
    "build": "tsc",
    "copy:slackbot": "cpy 'SlackBot/function.json' 'dist/SlackBot'",
    "copy:githubapp": "cpy 'GithubApp/function.json' 'dist/GithubApp'",
    "postbuild": "npm run copy:slackbot && npm run copy:githubapp",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start --script-root dist",
    "test": "jest --watch",
    "test:ci": "jest"
  },
  "dependencies": {
    "@azure/functions": "^1.2.3",
    "@octokit/auth": "^3.0.3",
    "@octokit/graphql": "^4.6.1",
    "@octokit/graphql-schema": "^10.25.1",
    "@octokit/webhooks": "^8.4.1",
    "@slack/bolt": "^3.2.0",
    "bolt-azure-functions-receiver": "^0.0.2",
    "dotenv": "^8.2.0",
    "typescript": "^4.2.2"
  },
  "devDependencies": {
    "@ffflorian/jszip-cli": "^3.1.0",
    "@types/jest": "^26.0.20",
    "azure-functions-core-tools": "^3.0.0",
    "cpy-cli": "^3.1.1",
    "jest": "^26.6.3",
    "mkdirp": "^1.0.4",
    "ts-jest": "^26.5.2",
    "ts-node": "^9.1.1"
  }
}
